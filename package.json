{
  "name": "ai-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that proxies AI API requests so static frontends never hold an API key",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit",
    "test:build": "tsc -p tsconfig.test.json",
    "test": "npm run test:build && node --test test/*.test.js"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260714.1",
    "typescript": "^5.5.4",
    "wrangler": "^4.0.0"
  }
}
